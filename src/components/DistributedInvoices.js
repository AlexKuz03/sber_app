import React from 'react';

const DistributedInvoices = () => {
    return (
        <div>
            {
              // тут что то будет
            }
        </div>
    );
};

export default DistributedInvoices;
